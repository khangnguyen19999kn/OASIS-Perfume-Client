import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  dropdown: {
    width: '100%',
    height: '100%',
    position: 'relative',
    top: '5px !important',
    left: '0 !important'
  }
}))
